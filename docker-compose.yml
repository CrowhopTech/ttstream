services:
  redis:
    container_name: redis
    image: redis:alpine
    ports:
      - "6379:6379"
    restart: always
    # Uncomment to enable redis persistence
    # volumes:
    #   - redis_data:/data

  # icecast:
  #   image: libretime/icecast
  #   container_name: icecast
  #   restart: unless-stopped
  #   ports:
  #     - "8069:8000"
  #   environment:
  #     ICECAST_SOURCE_PASSWORD: ${ICECAST_PASSWORD}
  #     ICECAST_ADMIN_PASSWORD: ${ICECAST_PASSWORD}
  #     ICECAST_PASSWORD: ${ICECAST_PASSWORD}
  #     ICECAST_HOSTNAME: "${LOCAL_IP}"
  #     ICECAST_MAX_CLIENTS: "5"
  #     ICECAST_MAX_SOURCES: "1"

  webpage:
    container_name: webpage
    build:
      context: ./static_web_content
      dockerfile: Dockerfile
    ports:
      - "5000:80"

  # icecast_audio_pusher:
  #   container_name: icecast_audio_pusher
  #   build:
  #     context: ./icecast_audio_pusher
  #     dockerfile: Dockerfile
  #   restart: always
  #   depends_on:
  #     - redis
  #     - icecast
  #   environment:
  #     - REDIS_ADDRESS=redis
  #     - ICECAST_ADDRESS=icecast
  #     - ICECAST_PORT=8000
  #     - ICECAST_PASSWORD=${ICECAST_PASSWORD}

  orchestrator:
    build:
      context: ./orchestrator
      dockerfile: Dockerfile
    container_name: orchestrator
    restart: unless-stopped
    ports:
      - "8888:8888"
    depends_on:
      - redis

  # openai_text_generator:
  #   container_name: openai_text_generator
  #   build:
  #     context: ./openai_text_generator
  #     dockerfile: Dockerfile
  #   restart: always
  #   depends_on:
  #     - redis
  #   environment:
  #     - REDIS_ADDRESS=redis
  #     - REDIS_PORT=6379
  #     - PROMPT_FILE=${PROMPT_FILE}
  #     - LLAMA_MODEL=${OPENAI_MODEL_FILE}
  #     - LLAMA_SERVER=llama_server
  #     - LLAMA_PORT=8080
  #   volumes:
  #     - ./openai_text_generator/prompts:/app/prompts

  # elevenlabs_tts_speaker:
  #   container_name: elevenlabs_tts_speaker
  #   build:
  #     context: ./elevenlabs_tts_speaker
  #     dockerfile: Dockerfile
  #   restart: always
  #   depends_on:
  #     - redis
  #   environment:
  #     - REDIS_ADDRESS=redis
  #     - REDIS_PORT=6379
  #     - ELEVENLABS_API_KEY=${ELEVENLABS_API_KEY}
  #   volumes:
  #     - ./elevenlabs_tts_speaker/voices.json:/app/voices.json

  # qwen_tts_speaker:
  #   container_name: qwen_tts_speaker
  #   build:
  #     context: ./qwen_tts_speaker
  #     dockerfile: Dockerfile
  #   restart: always
  #   depends_on:
  #     - redis
  #   environment:
  #     - REDIS_ADDRESS=redis
  #     - REDIS_PORT=6379
  #     - QWEN_TTS_MODEL=${QWEN_TTS_MODEL}
  #     - TTS_VOICE=${TTS_VOICE}
  #   volumes:
  #     - ./qwen_tts_speaker/audio_samples:/app/audio_samples
  #     - hf_cache:/root/.cache/huggingface/hub
  #   deploy:
  #     resources:
  #       reservations:
  #         devices:
  #           - driver: nvidia
  #             count: 1
  #             capabilities: [ gpu ]

volumes:
  redis_data:
  hf_cache:
