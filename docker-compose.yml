version: '3.8'

services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    expose:
      - "6379"
    restart: always
    networks:
      - redis-network
    volumes:
      - redis_data:/data

volumes:
  redis_data:

networks:
  redis-network:
